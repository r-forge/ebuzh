## fully import the required base packages and the survival framework
import(grDevices, graphics, stats, survival)

## bootstrapping facilities
importFrom(boot, boot, boot.ci)

## which functions shall be accessible by the users?
export(
    Aalpha,
    agresti,
    AUCbinorm,
    BlandAltman,
    behrensTest,
    clopperPearson,
    computeICCrater,
    confIntAUC,
    confIntPairedAUCDiff,
    confIntIndependentAUCDiff,
    confIntAUCbinorm,
    confIntDiagnostic,
    confIntFisherTrafo,
    confIntICC,
    confIntIndependentProportion,
    confIntIndependentDiagnostic,
    confIntIndependentAUCDiff,
    confIntKappa,
    confIntKM_t0,
    confIntMedian,
    confIntPairedProportion,
    confIntPairedAUCDiff,
    confIntPairedDiagnostic,
    confIntProportion,
    confIntRiskDiff,
    confIntRiskRatio,
    ConvertWeibull,
    faganLine,
    faganPlot,
    formatCI,
    formatPercent,
    formatPval,
    ilogit, expit,
    jeffrey, jeffreys,
    lamAlpha,
    logit,
    mantelCoxHR,
    mantelHaenszel,
    plotcorr,
    populationSamplePlot,
    quantileCumInc,
    quantileKM,
    rootFct,
    sampleSizeMcNemar,
    sampleSizePhase2BinaryTest,
    sampleSizePhase2BinaryTestSimulated,
    sampleSizeWilcoxTwoSample,
    standardErrorAUC,
    standardErrorAUCDiff,
    survrep,
    summaryROC,
    tableRegression,
    wald,
    WeibullDiag,
    WeibullReg,
    wilson
)
